name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore SystemCleaner.sln

      - name: Build
        run: dotnet build SystemCleaner.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test SystemCleaner.sln --configuration Release --no-build --verbosity normal

      - name: Publish
        run: dotnet publish SystemCleaner.App/SystemCleaner.App.csproj -c Release -r win-x64 --self-contained false -o publish

      - name: Archive Artifacts
        run: Compress-Archive -Path publish\* -DestinationPath SystemCleaner-App.zip
        shell: pwsh

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload Release Artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: SystemCleaner-App.zip
          asset_name: SystemCleaner-App.zip
          asset_content_type: application/zip
